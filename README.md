# i18n

```bash
Usage: i18n [OPTIONS] COMMAND [ARGS]...

  i18n helper script

Options:
  --help  Show this message and exit.

Commands:
  export     Exports the translations to the given filename
  translate  Generates the translation file for each languaje
```

# Instalación
```bash
pip install --editable .
```
